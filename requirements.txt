# Slim requirements used by Vercel serverless functions.
# Vercel limits each Python function to 250 MB unzipped, so heavy optional
# packages (opencv, numpy, scipy, scikit-learn) are NOT installed here.
# The app degrades gracefully without them (heuristic ML fallbacks; OCR is
# unavailable on Vercel anyway because the tesseract binary is missing).
# For full local functionality use:  pip install -r requirements-full.txt

# Certificate checker (Flask)
Flask>=3.0,<4
Werkzeug>=3.0
Pillow>=10,<13

# Tribal Scholar backend (FastAPI)
fastapi>=0.110.0
sqlalchemy>=2.0.27
pydantic>=2.6.1
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1
python-multipart>=0.0.9
rapidfuzz>=3.6.1
email-validator>=2.1.0
python-dateutil>=2.8.2
